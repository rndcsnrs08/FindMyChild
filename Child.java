class Child {
	
	//use geoLite lookup service to get this
	Location childLocation;
	String name;
	
	//may have to change name of function to match that in parent class
	Location showLocation (
		updateLocation();
		return Location;
	}
	
	String getName () {return name;};
	String setName(String nm) {name = nm;};
}